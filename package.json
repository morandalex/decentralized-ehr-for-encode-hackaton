{
  "name": "nextjs-ethereum-starter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "yarn workspace dehr-frontend dev",
    "build": "yarn workspace dehr-frontend build",
    "start": "yarn workspace dehr-frontend start",
    "chain": "yarn workspace dehr-contracts chain",
    "deploy": "yarn workspace dehr-contracts deploy",
    "compile": "yarn workspace dehr-contracts compile"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/hardhat",
      "**/hardhat/**"
    ]
  }
}
